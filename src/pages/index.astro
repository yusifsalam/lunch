---
import Layout from "@/layouts/Layout.astro";
import SessionView from "@/components/SessionView.tsx";

const user = Astro.locals.user;
const initial = await Astro.locals.service.snapshot(user.name);
---

<Layout title="Lunch">
  <SessionView client:load initial={initial} user={user} />
</Layout>
